{
  "name": "mca-ai-chat-root",
  "description": "Root package for MCA AI Chat project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install": "cd app && npm install",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "lint": "cd app && npm run lint",
    "preview": "cd app && npm run preview",
    "audit": "cd app && npm audit",
    "audit-fix": "cd app && npm audit fix"
  }
}
